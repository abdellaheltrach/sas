const PromptSync = require("prompt-sync")();

let a = Number(PromptSync("Entrez un nombre "));

if (a % 2 === 0) {
    console.log(`le numbers ${a} et pair.`);

} else {
    console.log(`le numbers ${a} et pair.`);
}